{
  "name": "meli-test-product-mngr",
  "version": "1.0.0",
  "description": "Manager that query api products",
  "author": "Julian Villegas",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "dev": "ts-node-dev --pretty src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -c tslint.json -p tsconfig.json"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node-dev": "^1.1.8",
    "ts-standard": "^11.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "express": "4.18.0"
  }
}
